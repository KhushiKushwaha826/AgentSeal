"""
audit.py

Placeholder for future audit-log features, such as keeping a history
of every verification check that was ever run (who checked what,
when, and what the result was).

Not needed for the first version — right now, verification results
are simply returned directly to whoever calls the /verify endpoint.

TODO (later, not now):
- Add an "AuditLog" table.
- Record every verification attempt (timestamp, decision_id, result).
"""

# Nothing to implement yet.
