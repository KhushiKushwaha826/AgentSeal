"""
helpers.py

Small general-purpose helper functions that don't belong to any
specific feature. Currently empty since we haven't needed any yet —
kept here so future helpers have an obvious home instead of getting
scattered across route/service files.
"""

# Nothing to implement yet.
